var React = require('react'),
    CartStore = require('../stores/CartStore');

var Cart= React.createClass({

  render: function () {
    return (
      <div>
        <h2>Cart</h2>
      </div>
    );
  }

});

module.exports = Cart;
